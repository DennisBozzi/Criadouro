import 'package:flutter/material.dart';

class femea extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(
          'Fêmeas',
          textAlign: TextAlign.center,
        ),
      ),
      body: Row(),
    );
  }
}
